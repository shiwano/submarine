// This file was generated by typhen-api

package {{lowerCamelCase parentModule}}

import (
	"errors"
)

type {{upperCamelCase name}} int

func (i {{upperCamelCase name}}) String() string {
	switch i {
	{{#each members}}
	case {{upperCamelCase ../name}}_{{upperCamelCase name}}:
		return "{{value}}"
	{{/each}}
	}
	return ""
}

func (i {{upperCamelCase name}}) Coerce() error {
	switch i {
	{{#each members}}
	case {{upperCamelCase ../name}}_{{upperCamelCase name}}:
		return nil
	{{/each}}
	}
	return errors.New("No {{upperCamelCase name}}")
}

const (
	{{#each members}}
	{{#if comment}}
	// {{comment}}
	{{/if}}
	{{upperCamelCase ../name}}_{{upperCamelCase name}} {{upperCamelCase ../name}} = {{value}}
	{{/each}}
)
