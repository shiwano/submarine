class {{controllerName this}}Controller < ApplicationController
  prepend TyphenApi::Controller::{{upperCamelCase fullName}}
  prepend TyphenApiRespondable

  def service
    # Implement here.
    render_response({{#each callSignatures.0.returnType.properties}}{{serializablePropertyName this}}: nil{{#unless @last}}, {{/unless}}{{/each}})
  end
end
